<tr>
    <td><%= customer.id %></td>
    <td><%= customer.name %></td>
    <td><%= customer.email %></td>
    <td>
      <%= link_to t('orders'), admin_orders_path(
            :customer_id => customer.id
        ) %>
    </td>
</tr>

